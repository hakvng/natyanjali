<% provide(:title, 'All users') %>
<h1>Students</h1>

<ul class="users">
  <% @users.each do |user| %>
    <% if not user.admin? %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, user %>
    </li>
    <% end %>
  <% end %>
</ul>
